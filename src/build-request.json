{
  "kind": "build_request",
  "title": "Publish the current site online (production/public)",
  "priority": "normal",
  "requirements": [],
  "constraints": [
    "Use the canonical production slug exactly as defined in frontend/domain-slug.json (\"galfashionworld-2014\") for any production/public publish workflow so slug validation passes.",
    "Do not modify files under frontend/src/components/ui or any other frontend immutable paths (compose them instead).",
    "Keep backend as a single Motoko actor; do not introduce additional backend services."
  ],
  "nonGoals": [
    "No new UI/UX or copy changes as part of this request.",
    "No new backend features or data persistence changes as part of this request.",
    "No image generation or asset edits as part of this request."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}